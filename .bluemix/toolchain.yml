---
build:
  capability: pipeline
  metadata: pipeline.yml
  env:
    SAUCE_USER: ${test.parameters.username}
    API_KEY: ${test.parameters.key}
test:
  capability: saucelabs
  metadata: saucelabs.yml
candy:
  capability: chocolate
  metadata: chocolate.yml
messaging:
  capability: slack
  metadata: slack.yml
